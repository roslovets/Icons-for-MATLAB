function is = icons(packs)
if nargin > 0 && ~isempty(packs)
    packs = string(packs);
else
    packs = string(fa.packs);
end
if ~isempty(packs)
    for i = 1 : length(packs)
    fs = dir(fullfile(fa.Util.getiroot, packs(1), '*.png'));
    fs = string({fs.name})';
    is = erase(fs, '.png');
    end
else
    is = {};
end